# Unbind the default prefix
unbind C-b

# Set new prefix to Ctrl+a
set-option -g prefix C-a

# Bind the new prefix to send prefix to nested tmux (optional, useful for nested sessions)
bind C-a send-prefix

